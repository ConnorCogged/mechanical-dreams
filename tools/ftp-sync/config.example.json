{
  "//": "Copy this file to config.json and fill in real values. config.json is gitignored.",
  "//protocol": "ftp (default) or sftp. Pterodactyl panels usually expose SFTP on port 2022.",
  "protocol": "ftp",

  "//host": "Hostname or IP of the server (no protocol prefix).",
  "host": "ftp.example-host.net",

  "//port": "FTP plain/explicit-TLS is usually 21. Pterodactyl SFTP is usually 2022.",
  "port": 21,

  "//user": "FTP/SFTP username. Pterodactyl SFTP usernames look like 'user.serverid'.",
  "user": "myuser",

  "//password": "FTP/SFTP password. Never commit this.",
  "password": "changeme",

  "//secure": "FTP only. true = explicit FTPS (FTP over TLS). false = plain FTP. Ignored for sftp.",
  "secure": false,

  "//remoteBase": "Remote directory the pack lives in. mods/ and config/ are created/synced under this. Typically '/' for Pterodactyl (the server root).",
  "remoteBase": "/",

  "//pterodactyl": "Used by the RECOMMENDED --deploy path (zip + decompress via the Pterodactyl client API). The FTP/SFTP fields above are only used by the --upload fallback.",
  "pterodactyl": {
    "//panelUrl": "Base URL of your Pterodactyl panel, no trailing slash, e.g. 'https://panel.your-host.net'.",
    "panelUrl": "https://panel.example-host.net",

    "//apiKey": "A CLIENT API key (Account -> API Credentials in the panel). Starts with 'ptlc_'. Never commit this.",
    "apiKey": "ptlc_changeme",

    "//serverId": "The short server identifier from the panel URL (panel.host/server/<serverId>), e.g. 'a1b2c3d4'.",
    "serverId": "a1b2c3d4",

    "//remoteModsDir": "Mods directory relative to the server root. Default 'mods'.",
    "remoteModsDir": "mods"
  }
}
